<Window x:Class="RevitMcp.Plugin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit MCP Server"
        Width="700" Height="520"
        MinWidth="520" MinHeight="380"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        FontFamily="Segoe UI"
        FontSize="13">

    <Window.Resources>
        <!-- Colours -->
        <SolidColorBrush x:Key="Surface"      Color="#2A2A3E"/>
        <SolidColorBrush x:Key="Accent"       Color="#3B82F6"/>
        <SolidColorBrush x:Key="AccentHover"  Color="#60A5FA"/>
        <SolidColorBrush x:Key="Danger"       Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerHover"  Color="#F87171"/>
        <SolidColorBrush x:Key="TextPrimary"  Color="#E0E0F0"/>
        <SolidColorBrush x:Key="TextMuted"    Color="#888899"/>
        <SolidColorBrush x:Key="GreenDot"     Color="#4ADE80"/>
        <SolidColorBrush x:Key="RedDot"       Color="#F87171"/>

        <!-- Button base style -->
        <Style x:Key="McpButton" TargetType="Button">
            <Setter Property="Foreground"           Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness"      Value="0"/>
            <Setter Property="Padding"              Value="22,8"/>
            <Setter Property="Cursor"               Value="Hand"/>
            <Setter Property="FontSize"             Value="13"/>
            <Setter Property="FontWeight"           Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Header ── -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Text="Revit MCP Server" FontSize="20" FontWeight="Bold"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Model Context Protocol · Revit Bridge"
                           Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>

            <!-- Status badge -->
            <Border Grid.Column="1" CornerRadius="12" Padding="14,6"
                    Background="{StaticResource Surface}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="9" Height="9" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{StaticResource RedDot}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                        <Setter Property="Fill" Value="{StaticResource GreenDot}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextPrimary}"
                               FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ── Info panel ── -->
        <Border Grid.Row="2" Background="{StaticResource Surface}" CornerRadius="8" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- URL -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="SERVER URL" Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding ServerUrl, Mode=OneWay}"
                             IsReadOnly="True" Background="Transparent"
                             BorderThickness="0" Foreground="#60A5FA"
                             FontFamily="Consolas" FontSize="13"
                             Padding="0,4,0,0" Cursor="IBeam"/>
                </StackPanel>

                <!-- Divider -->
                <Rectangle Grid.Column="1" Width="1" Fill="#3A3A50" HorizontalAlignment="Center"/>

                <!-- Revit connection -->
                <StackPanel Grid.Column="2" Margin="20,0,0,0">
                    <TextBlock Text="REVIT CONNECTION" Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="{StaticResource TextPrimary}"
                               Padding="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Log area ── -->
        <Border Grid.Row="4" Background="{StaticResource Surface}" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Log header -->
                <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="#3A3A50" Padding="14,8">
                    <TextBlock Text="Activity Log" Foreground="{StaticResource TextMuted}"
                               FontSize="11" FontWeight="SemiBold"/>
                </Border>

                <ListBox Grid.Row="1" ItemsSource="{Binding LogEntries}"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{StaticResource TextPrimary}"
                         FontFamily="Consolas" FontSize="11.5"
                         Padding="10,6"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         x:Name="LogList">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding"    Value="4,1"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- ── Toolbar ── -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="▶  Start"
                    Style="{StaticResource McpButton}"
                    Background="{StaticResource Accent}"
                    Command="{Binding StartCommand}"/>

            <Button Grid.Column="2" Content="■  Stop"
                    Style="{StaticResource McpButton}"
                    Background="{StaticResource Danger}"
                    Command="{Binding StopCommand}"/>

            <Button Grid.Column="4" Content="Clear Log"
                    Style="{StaticResource McpButton}"
                    Background="#3A3A50"
                    Command="{Binding ClearLogCommand}"/>
        </Grid>
    </Grid>
</Window>
